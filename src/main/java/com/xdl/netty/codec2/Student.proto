syntax = "proto3";
option java_outer_classname = "StudentPOJO"; //生成的外部类名，同时也是文件名
// proto使用message管理数据
message Student { // 会在StudentPOJO类中生成一个内部类Student
  int32 id = 1; // 1表示序号，不是值
  string name = 2;
}